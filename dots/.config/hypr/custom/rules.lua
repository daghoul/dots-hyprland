-- This file will not be overwritten across dots-hyprland updates.
-- The file name is for the sake of organization and does not matter
-- See the corresponding files in ~/.config/hypr/hyprland for examples
-- PCManFM
hl.window_rule({ match = { title = "^(Mount)$" }, center = true })
hl.window_rule({ match = { title = "^(Mount)$" }, float = true })
hl.window_rule({ match = { title = "^(Execute File)$" }, center = true })
hl.window_rule({ match = { title = "^(Execute File)$" }, float = true })
hl.window_rule({ match = { title = "^(Delete Files)$" }, center = true })
hl.window_rule({ match = { title = "^(Delete Files)$" }, float = true })
hl.window_rule({ match = { title = "^(Delete Files)$" }, no_initial_focus = true })
hl.window_rule({ match = { title = "^(File Properties)$" }, center = true })
hl.window_rule({ match = { title = "^(File Properties)$" }, float = true })
hl.window_rule({ match = { title = "^(Search Files)$" }, center = true })
hl.window_rule({ match = { title = "^(Search Files)$" }, float = true })
hl.window_rule({ match = { title = "^(Choose an Application)$" }, center = true })
hl.window_rule({ match = { title = "^(Choose an Application)$" }, float = true })